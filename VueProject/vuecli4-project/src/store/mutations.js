/**
 *直接更新state.js文件中的数据
 */
import {} from './mutation-types'

export default {
  /**
   * [常量名](state,{ actions.js文件中commit方法中的定义的变量名（一般跟state中的数据名同名）}) {
   *    //将服务端请求过来的也就是actions.js文件中commit方法中定义的变量所带有的数据 赋值给state中定义的数据
   *    state.数据名 = actions.js文件中commit方法中的定义的变量（已经被赋值了从服务端请求的数据）
   * }
   */
}